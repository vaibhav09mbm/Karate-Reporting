<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">

    
        <f:entry title="${%reportTitle.title}"
                 field="reportTitle">
            <f:textbox/>
        </f:entry>

         <f:entry title="${%reportDirectory.title}"
                     field="reportDirectory">
                <f:textbox/>
            </f:entry>

</j:jelly>
